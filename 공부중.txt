System.out.println("당신의 점수를 입력하세요")

int a = sc.nextint() - 국어
int b = sc.nextint() - 영어
int c = sc.nextint() - 수학
double d = (a+b+c)/3.0 ;

System.out.println ("당신의 국어점수는" + a + "이고 영어 점수는" +
b + "이며 수학점수는" +c + "입니다.")
System.out.println("총점은" + (a+b+c) +"이고 평균은"+ d +"이며
등급은 " + 

x,y좌표가 점 (50,50) (100,100)직사각형을 이루는 점안에 있는지 확인하여라

1. 정수 n이 1보다 크고 10보다 작다고 했을 때
정수 n을 3으로 나눈 나머지가 0이면 박수짝()이라고 출력이 되고 아니라면 
박수 없음이라고 출력이 된다.

if 문 간단설명
if ( ) : if문 안에 있는 괄호가 참이라면 내가 출력을 설정해놓은 값이 나올것이고
참이 아니라면 출력이 되지않는다.

if(){출력 값
} else(){ 출력값}
if 문 안에있는 괄호가 참이라면 그 출력값이 나올것이고
if 가 거짓이라면 else 문의 출력값이 나올 것 이다.

if ~ else if ~ else 
: if 와 else 는 단 한번만 사용 가능하며 if 로 시작을해야하며 else if 는 여러번 사용 가능
거짓인 블록은 건너 뛰고 다음 조건을 실행